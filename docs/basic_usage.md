Things to document:

Why running libcamera in Poetry creates weird problems

Why we have to install an aarch64 cross-compiler on aarch64

Why I made main.py a monolith

Who actually made this insane asylum

Why we target ***only*** Raspberry Pi 3s running Ubuntu Server 25.10

Why you may need to add new package indexes for this to work

Why we used Tkinter

Why we included wheel and Poetry as runtime depenendencies.

